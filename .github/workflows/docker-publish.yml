on:
  push:
    branches:
      - master
    paths-ignore:
      - "**/README*"

permissions:
  contents: read
  packages: write

jobs:
  docker-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/bake-action@v6
        with:
          push: true
          set: |
            frontend.tags=ghcr.io/migalmoreno/tubo-frontend:latest
            backend.tags=ghcr.io/migalmoreno/tubo-backend:latest
            bg-helper.tags=ghcr.io/migalmoreno/tubo-bg-helper:latest
            *.platform+=linux/amd64
            *.platform+=linux/arm64
            *.cache-from=type=gha
            *.cache-to=type=gha,mode=max
